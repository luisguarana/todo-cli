---
phase: 01-core-cli
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: true
requirements:
  - CMD-01
  - CMD-02
  - CMD-03

must_haves:
  truths:
    - "Running `npm link` (or `npm install -g .`) after this plan makes `todo` available as a global command"
    - "package.json bin field maps the name `todo` to `./todo.js`"
    - "Existing package.json fields (scripts.prepare, devDependencies, type) are unchanged"
  artifacts:
    - path: "package.json"
      provides: "bin field wiring todo command to todo.js"
      contains: "\"bin\""
  key_links:
    - from: "package.json bin.todo"
      to: "todo.js"
      via: "npm link resolution"
      pattern: "\"todo\".*\"./todo\\.js\""
---

<objective>
Add the `bin` field to package.json so the `todo` command is available after `npm link` or global install.

Purpose: Wire the CLI entry point so users can invoke `todo` directly without `node todo.js` (per D-03).
Output: package.json updated with `"bin": { "todo": "./todo.js" }`, all other fields preserved.
</objective>

<execution_context>
@C:/Users/nextl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nextl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-cli/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bin field to package.json</name>
  <files>package.json</files>

  <read_first>
  - C:\Users\nextl\Desktop\AI_Workspace\package.json — read full current content before editing; confirm "type": "commonjs", "scripts": { "prepare": "husky" }, devDependencies shape
  </read_first>

  <action>
  Edit package.json to add a top-level `"bin"` field with value `{ "todo": "./todo.js" }` (per D-03).

  Rules:
  - Do NOT modify `"scripts"` — preserve `"prepare": "husky"` exactly (the husky hook must survive)
  - Do NOT modify `"devDependencies"` — husky stays
  - Do NOT modify `"type": "commonjs"`
  - Place the `"bin"` field after the `"main"` field for conventional ordering
  - Result: `"bin": { "todo": "./todo.js" }` present in package.json

  After editing, the user can run `npm link` from the workspace root to make `todo` available globally on their PATH.
  </action>

  <verify>
    <automated>node -e "const p = require('./package.json'); console.log(p.bin && p.bin.todo === './todo.js' ? 'PASS: bin wired' : 'FAIL: bin missing or wrong')"</automated>
  </verify>

  <acceptance_criteria>
  - `package.json` contains `"bin": { "todo": "./todo.js" }`
  - `package.json` still contains `"prepare": "husky"` under scripts
  - `package.json` still contains `"type": "commonjs"`
  - `package.json` still contains `"husky"` under devDependencies
  - Running `node -e "const p=require('./package.json'); process.exit(p.bin && p.bin.todo === './todo.js' ? 0 : 1)"` exits with code 0
  </acceptance_criteria>

  <done>package.json has bin field mapping "todo" to "./todo.js"; all pre-existing fields intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package.json bin → PATH | npm link exposes todo.js on system PATH after this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | npm link PATH registration | accept | Developer tool, user-initiated install — standard npm bin behavior |
| T-02-SC | Tampering | npm/pip install | accept | No new packages installed in this plan — bin field only |
</threat_model>

<verification>
After this plan completes:
1. `node -e "const p=require('./package.json'); console.log(JSON.stringify(p.bin))"` outputs `{"todo":"./todo.js"}`
2. `node -e "const p=require('./package.json'); console.log(p.scripts.prepare)"` outputs `husky`
3. (Optional, not required for plan completion) `npm link` from workspace root registers `todo` globally
</verification>

<success_criteria>
- package.json bin field present and correct
- No existing fields were removed or changed
- npm link would succeed (no syntax errors in package.json)
</success_criteria>

<output>
Create `.planning/phases/01-core-cli/01-02-SUMMARY.md` when done.
</output>

---

## Artifacts This Phase Produces

| Symbol / Path | Type | Description |
|---------------|------|-------------|
| `package.json` (modified) | Config | Added `"bin": { "todo": "./todo.js" }` field enabling global install |
