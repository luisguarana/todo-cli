---
phase: 02-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - todo.js
autonomous: true
requirements:
  - CMD-04
  - ERR-01
  - ERR-02

must_haves:
  truths:
    - "Running `node todo.js` (no subcommand) prints a usage block and exits with code 0"
    - "Running `node todo.js foo` (unknown subcommand) prints a usage block and exits with code 0"
    - "Running `node todo.js done 99` when ID 99 does not exist prints a not-found message and exits with code 1"
    - "Running `node todo.js list` when todos.json is absent prints 'No todos yet.' and exits 0"
    - "Running `node todo.js done 1` when todos.json is absent prints a not-found message and exits with code 1"
  artifacts:
    - path: "todo.js"
      provides: "Updated CLI with usage help, ERR-01 guard, and ERR-02 guard"
      contains: "process.exit"
  key_links:
    - from: "command dispatch block (process.argv[2])"
      to: "printUsage() + process.exit(0)"
      via: "else/default branch"
      pattern: "printUsage"
    - from: "done command handler"
      to: "process.exit(1)"
      via: "index === -1 guard"
      pattern: "process\\.exit\\(1\\)"
---

<objective>
Harden todo.js so every input path — unknown commands, missing todos.json, and nonexistent IDs — produces a clear, human-readable message instead of a crash or silent failure.

Purpose: Phase 1 ships the happy path. Phase 2 makes the tool safe to use in the real world where files may not exist yet and users mistype commands.

Output: Updated todo.js with three new behaviors: usage help on unknown/no command (CMD-04), not-found guard on `done` with a bad ID (ERR-01), and graceful empty-state on `done` when todos.json is absent (ERR-02, extends existing `list` ENOENT handling already present).
</objective>

<execution_context>
@C:/Users/nextl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nextl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/nextl/Desktop/AI_Workspace/.planning/ROADMAP.md
@C:/Users/nextl/Desktop/AI_Workspace/.planning/REQUIREMENTS.md
@C:/Users/nextl/Desktop/AI_Workspace/.planning/STATE.md
</context>

## Artifacts this phase produces

| Symbol | File | Type | Description |
|--------|------|------|-------------|
| `printUsage()` | todo.js | function | Prints usage block to stdout; called on no/unknown command |
| usage exit branch | todo.js | control flow | `else` branch at end of command dispatch — calls `printUsage()` then `process.exit(0)` |
| ID-not-found guard | todo.js | control flow | Check `index === -1` inside `done` handler before splice — prints message and calls `process.exit(1)` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add usage help, ID guard, and missing-file graceful handling to todo.js</name>
  <files>todo.js</files>

  <read_first>
    Read C:/Users/nextl/Desktop/AI_Workspace/todo.js in full before touching anything.
    Key observations to confirm before writing:
    - Line 13-15: ENOENT catch in readTodos() already returns [] — list already handles missing file
    - Line 43-53: done handler — findIndex may return -1; todos[-1] is undefined; removed.text crashes
    - Line 24 onward: dispatch is if/else if chain with no final else branch (no unknown-command path)
  </read_first>

  <behavior>
    - `node todo.js` (argv[2] is undefined) → stdout contains "Usage:" line, exit code 0
    - `node todo.js foobar` (unknown command) → stdout contains "Usage:" line, exit code 0
    - `node todo.js done 99` when todos.json has no item with id 99 → stdout contains "No todo with ID 99", exit code 1
    - `node todo.js done 1` when todos.json does not exist → stdout contains "No todo with ID 1", exit code 1 (readTodos returns [], findIndex returns -1, same guard fires)
    - `node todo.js list` when todos.json does not exist → "No todos yet." — already works; must not regress
    - `node todo.js add "x"` / `node todo.js done 1` (valid) → existing behavior unchanged, exit code 0
  </behavior>

  <action>
    Add a `printUsage()` function near the top of todo.js (after the TODOS_PATH declaration, before readTodos). The function writes three lines to stdout using console.log:
      Line 1: "Usage:"
      Line 2: "  todo add <text>    Add a new to-do item"
      Line 3: "  todo list          List all to-do items"
      Line 4: "  todo done <id>     Mark item done (removes it)"

    In the done handler: immediately after computing `index` with findIndex, add an if-block that checks `index === -1`. Inside that block: print "No todo with ID " + id + "." using console.log, then call process.exit(1). The splice and subsequent lines execute only when index is a valid non-negative integer.

    At the end of the command dispatch chain: add a final else branch (covering both undefined command and unrecognized strings). Inside: call printUsage(), then call process.exit(0).

    No changes to readTodos(), writeTodos(), add handler, or list handler.
    No new require() calls — fs and path are still the only requires.
    CommonJS style throughout (no import/export, no arrow-function declarations at top level unless already present).
  </action>

  <verify>
    <automated>
      node -e "const {execSync}=require('child_process'); const r=(cmd,code)=>{try{const o=execSync('node todo.js '+cmd,{cwd:'C:/Users/nextl/Desktop/AI_Workspace',encoding:'utf8'});if(code!==0)throw new Error('expected exit '+code+' for: '+cmd);return o;}catch(e){if(e.status===undefined)throw e;if(e.status!==code)throw new Error('exit '+e.status+' expected '+code+' for: '+cmd);return e.stdout||'';}}; let out; out=r('',0); if(!out.includes('Usage:'))throw new Error('no Usage on bare call'); out=r('foobar',0); if(!out.includes('Usage:'))throw new Error('no Usage on unknown cmd'); out=r('done 9999',1); if(!out.includes('No todo with ID 9999'))throw new Error('missing not-found message: '+out); console.log('ALL PASS');"
    </automated>
  </verify>

  <done>
    - `node todo.js` prints a block containing "Usage:" and exits 0
    - `node todo.js foobar` prints a block containing "Usage:" and exits 0
    - `node todo.js done 9999` (nonexistent ID) prints "No todo with ID 9999." and exits 1
    - `node todo.js done 1` when todos.json is absent (readTodos returns []) prints "No todo with ID 1." and exits 1
    - `node todo.js list` when todos.json is absent prints "No todos yet." and exits 0 (existing behavior retained)
    - `node todo.js add "test"` and `node todo.js done 1` (valid) continue to work as before
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → process | process.argv values are user-controlled strings read before any file I/O |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | process.argv[3] (id arg to done) | accept | parseInt with radix 10 already in place; NaN id produces findIndex === -1 which now exits cleanly instead of crashing |
| T-02-02 | Denial of Service | todos.json unreadable (permissions) | accept | readTodos re-throws non-ENOENT errors; stack trace on permission error is acceptable for a local single-user CLI |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase — Node.js built-ins only (CON-01) |
</threat_model>

<verification>
Run all three behavioral checks in sequence:

1. `node todo.js` — stdout must contain "Usage:", exit code must be 0
2. `node todo.js unknowncmd` — stdout must contain "Usage:", exit code must be 0
3. `node todo.js done 9999` — stdout must contain "No todo with ID 9999", exit code must be 1
4. Temporarily rename todos.json if it exists, run `node todo.js done 1`, confirm exit 1 and message; restore file
5. `node todo.js list` (with todos.json absent or empty) — stdout "No todos yet.", exit 0
6. Full regression: `node todo.js add "smoke test"` → `node todo.js list` → `node todo.js done 1` — all exit 0, correct output
</verification>

<success_criteria>
Phase 2 is complete when:
- Running `todo` or `todo <unknown>` prints usage help (contains "Usage:") and exits 0 (CMD-04)
- Running `todo done <nonexistent-id>` prints a message containing the ID and exits 1 (ERR-01)
- Running `todo list` with no todos.json prints "No todos yet." and exits 0 (ERR-02 — list)
- Running `todo done <id>` with no todos.json exits 1 with a clear message, no stack trace (ERR-02 — done)
- All Phase 1 behaviors (add/list/done on valid data) continue to work without regression
- todo.js still uses only fs and path from Node.js built-ins (CON-01 maintained)
</success_criteria>

<output>
Create `.planning/phases/02-hardening/02-01-SUMMARY.md` when done
</output>
